import "./Editor.css";

const Editor = () => {
  return (
    <section className="Editor">
      <input placeholder="새로운 Todo..." />
      <button>추가</button>
    </section>
  );
};

export default Editor;
